"""Interacting with user, through terminal"""

# Regardless of the character entered, python interprets it as a string type.
# Simply, it converts any input entered by the user; to string
response = int(input("1 / 2?"))  # Converting the input
print(response)
